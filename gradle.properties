# 開啟 AndroidX 支援 (Jetpack Compose 必須)
android.useAndroidX=true

# 自動將舊版第三方庫轉換為 AndroidX (建議開啟)
android.enableJetifier=true

# 增加 Gradle 編譯記憶體，加快 GitHub Actions 打包速度
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
